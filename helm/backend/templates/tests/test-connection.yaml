apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "migration-crd.fullname" . }}-test-connection"
  labels:
    {{- include "migration-crd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "migration-crd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
